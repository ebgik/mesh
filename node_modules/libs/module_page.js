var db = require('libs/module_db');


exports.getPage = function(id,callback) {
	var query = db.query('SELECT * FROM page WHERE id='+id, function(err, result) {
	  if (result[0])
	  	callback(err,result[0]);
	  else 
	  	callback(err,'none');
	});
}
